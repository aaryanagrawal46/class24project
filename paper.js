class paper 
{
constructor(x,y,z)
{
var options{
    isStaticfalse:
    restitution:0.3,
friction:0.5,
density:1.2
    
}
this.x;
this.y;
this.z;

this.body.Bodies.circle(this.x,this.y,this.r,options)
World.add(world,this.body);
}
display()
{
    var paperposition.body.position;
    Push()
    transalate(paperpos.x,paperpos.y);
    rectMode(CENTER)
    

    )
}